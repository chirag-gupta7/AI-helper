# Core Flask Framework
flask==2.3.3
flask-cors==4.0.0
flask-socketio==5.3.6
werkzeug==2.3.7

# Database & Authentication
flask-sqlalchemy==3.1.1
flask-migrate==4.0.5
flask-login==0.6.2

# Environment Configuration
python-dotenv==1.0.0

# Google Calendar Integration 
google-auth==2.23.4
google-auth-oauthlib==1.1.0
google-api-python-client==2.134.0
python-dateutil==2.8.2

# ElevenLabs Voice API (Updated)
elevenlabs==2.7.1

# HTTP Requests
requests==2.31.0

# Rate Limiting (optional - can be removed if not using Redis)
flask-limiter==3.5.0

# Text-to-Speech (lightweight, no Rust dependencies)
pyttsx3==2.90

# Development/Testing (optional - can be removed for production)
pytest-flask==1.2.0

# Security
Flask-Talisman==1.1.0

# Enhanced Voice Command Dependencies
# Weather API support
pyowm==3.3.0  # Alternative: use requests directly with OpenWeatherMap API

# News API support  
newsapi-python==0.2.6  # Alternative: use requests directly with NewsAPI

# Background task processing (for reminders and timers)
APScheduler==3.10.4

# UUID support (built-in with Python 3.7+)
# uuid is built-in

# JSON handling (built-in)
# json is built-in

# Threading (built-in)
# threading is built-in